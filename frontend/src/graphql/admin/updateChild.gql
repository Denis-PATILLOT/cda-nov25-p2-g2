mutation UpdateChild($id: Int!, $data: UpdateChildInput!) {
  updateChild(id: $id, data: $data) {
    id
    firstName
    lastName
    birthDate
    picture
    healthRecord
    group {
      id
      name
    }
  }
}
